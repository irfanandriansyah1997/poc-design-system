import { css } from '@emotion/react';

export const styModalActionButton = css`
  .modal-action-button {
    &__item {
      cursor: pointer;
      border: 0;
      background: transparent;
      width: 44px;
      height: 44px;
    }
  }
`;
